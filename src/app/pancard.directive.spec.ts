import { PancardDirective } from './pancard.directive';

describe('PancardDirective', () => {
  it('should create an instance', () => {
    const directive = new PancardDirective();
    expect(directive).toBeTruthy();
  });
});
