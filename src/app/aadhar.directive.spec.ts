import { AadharDirective } from './aadhar.directive';

describe('AadharDirective', () => {
  it('should create an instance', () => {
    const directive = new AadharDirective();
    expect(directive).toBeTruthy();
  });
});
